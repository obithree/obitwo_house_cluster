---
all:
  vars:
    ansible_user: obitwo
  children:
    pc:
      hosts:
        ubuntu-pc:
          ansible_host: 192.168.100.100
          # ansible_become_password: "{{ vault_become_password }}"  # --ask-become-passを使用する場合はコメントアウト
    raspi:
      children:
        raspi-master:
          hosts:
            raspi01:
              ansible_host: 192.168.100.101
              hostname: raspi01.home
        raspi-worker:
          hosts:
            raspi02:
              ansible_host: 192.168.100.102
              hostname: raspi02.home
            raspi03:
              ansible_host: 192.168.100.103
              hostname: raspi03.home
