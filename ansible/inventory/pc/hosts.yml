---
all:
  children:
    pc:
      hosts:
        ubuntu-pc:
          ansible_host: 192.168.1.100  # PCのIPアドレスに変更してください
          ansible_user: ubuntu          # ユーザー名に変更してください
          ansible_become_password: "{{ vault_become_password }}"  # または直接パスワードを設定
