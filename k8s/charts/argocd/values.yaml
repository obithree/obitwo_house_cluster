# ArgoCD default values

ingress:
  enabled: true
  className: traefik
  host: argo-cd.home
  port: 80

service:
  name: argocd-server
  port: 80

annotations:
  nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
