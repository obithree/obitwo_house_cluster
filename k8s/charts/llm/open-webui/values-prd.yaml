replicaCount: 1

image:
  tag: "main"

service:
  type: ClusterIP

persistence:
  size: 20Gi

resources:
  requests:
    memory: "1Gi"
    cpu: "1000m"
  limits:
    memory: "2Gi"
    cpu: "2000m"

ollama:
  baseUrl: "http://ollama:11434"

ingress:
  enabled: true
  className: nginx
  host: open-webui.home
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
