apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  labels:
    app: litellm
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
data:
  config.yaml: |
    model_list:
    {{- range .Values.models }}
      - model_name: {{ .name }}
        litellm_params:
          model: openai/{{ .ollamaModel }}
          api_base: {{ $.Values.ollama.baseUrl }}/v1
          api_key: ollama
          drop_params: true
          extra_body:
            {{- toYaml $.Values.ollama.options | nindent 12 }}
        model_info:
          supports_reasoning: false
    {{- end }}
    litellm_settings:
      drop_params: true
      modify_params: true
      num_retries: 3
      request_timeout: 600
      default_litellm_params:
        drop_params: true
    router_settings:
      disable_reasoning: true
